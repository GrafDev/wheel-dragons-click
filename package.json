{
  "name": "wheel",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:all": "npm run build:button && npm run build:auto && npm run build:button-canada && npm run build:auto-canada",
    "deploy:all": "npm run build:all && firebase deploy",
    "build:button": "vite build --mode button",
    "build:auto": "vite build --mode auto",
    "build:button-canada": "vite build --mode button-canada",
    "build:auto-canada": "vite build --mode auto-canada",
    "archive": "npm run build:all && node scripts/build-archive.js button && node scripts/build-archive.js auto && node scripts/build-archive.js button-canada && node scripts/build-archive.js auto-canada && node scripts/client-archive.js"
  },
  "devDependencies": {
    "vite": "^6.0.11"
  },
  "dependencies": {
    "firebase": "^11.6.0",
    "gsap": "^3.12.7",
    "i18next": "^25.0.1"
  }
}
